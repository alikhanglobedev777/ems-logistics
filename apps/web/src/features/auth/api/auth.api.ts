export {
  AUTH_SESSION_CHANGED_EVENT,
  clearAuthSession,
  getMe,
  hasAuthSession,
  login,
  register,
  saveAuthSession,
} from '@ems/api-client';

export type { CurrentUser } from '@ems/api-client';
