import{useCreateCustomer,useGetCustomerById,useGetCustomers,useUpdateCustomer,type Customer}from'@ems/api-client';import{CustomerType}from'@ems/shared';import{z}from'zod';import{MasterDataPage,type FormValues}from'../components/master-data-page';
export function CustomersPage({mode,id}:{mode:'list'|'create'|'edit'|'detail';id?:string}){const list=useGetCustomers({page:1,limit:50});const detail=useGetCustomerById(Number(id??0),{query:{enabled:Boolean(id)}});const create=useCreateCustomer();const update=useUpdateCustomer();const rows:Customer[]=list.data?.status===200?list.data.data.data:[];const item=detail.data?.status===200?detail.data.data.data:undefined;const initial=item?{name:item.name,contactPerson:item.contactPerson,phone:item.phone,email:item.email??'',billingAddress:item.billingAddress??'',customerType:item.customerType,ntn:item.ntn??'',strn:item.strn??'',isActive:String(item.isActive)}:undefined;async function submit(v:FormValues){const data={name:v.name,contactPerson:v.contactPerson,phone:v.phone,email:v.email||null,billingAddress:v.billingAddress||null,customerType:v.customerType as Customer['customerType'],ntn:v.ntn||null,strn:v.strn||null,isActive:v.isActive!=='false'};if(mode==='edit')await update.mutateAsync({customerId:Number(id),data});else await create.mutateAsync({data});window.location.href='/customers';}return <MasterDataPage title="Customers" resource="customers" mode={mode} rows={rows} detail={item} loading={list.isLoading||detail.isLoading} columns={[{header:'Name',render:r=>r.name},{header:'Contact',render:r=>r.contactPerson},{header:'Phone',render:r=>r.phone},{header:'Type',render:r=>r.customerType}]} fields={[{name:'name',label:'Name'},{name:'contactPerson',label:'Contact person'},{name:'phone',label:'Phone'},{name:'email',label:'Email',type:'email'},{name:'billingAddress',label:'Billing address'},{name:'customerType',label:'Customer type',options:Object.values(CustomerType).map(v=>({label:v,value:v}))},{name:'ntn',label:'NTN'},{name:'strn',label:'STRN'},{name:'isActive',label:'Status',options:[{label:'Active',value:'true'},{label:'Inactive',value:'false'}]}]} schema={z.object({name:z.string().min(1),contactPerson:z.string().min(1),phone:z.string().min(1),email:z.string(),billingAddress:z.string(),customerType:z.string().min(1),ntn:z.string(),strn:z.string(),isActive:z.string()})} initial={initial} onSubmit={submit}/>;}
