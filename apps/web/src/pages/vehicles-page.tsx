import{useCreateVehicle,useGetVehicleById,useGetVehicles,useUpdateVehicle,type Vehicle}from'@ems/api-client';import{VehicleStatus}from'@ems/shared';import{z}from'zod';import{MasterDataPage,type FormValues}from'../components/master-data-page';
export function VehiclesPage({mode,id}:{mode:'list'|'create'|'edit'|'detail';id?:string}){const list=useGetVehicles({page:1,limit:50});const detail=useGetVehicleById(Number(id??0),{query:{enabled:Boolean(id)}});const create=useCreateVehicle();const update=useUpdateVehicle();const rows:Vehicle[]=list.data?.status===200?list.data.data.data:[];const item=detail.data?.status===200?detail.data.data.data:undefined;const initial=item?{vehicleNumber:item.vehicleNumber,vehicleTypeId:String(item.vehicleType.id),currentStationId:String(item.currentStation.id),status:item.status,fuelCardNumber:item.fuelCardNumber??'',registrationExpiry:item.registrationExpiry??'',fitnessExpiry:item.fitnessExpiry??'',insuranceExpiry:item.insuranceExpiry??'',isActive:String(item.isActive)}:undefined;async function submit(v:FormValues){const data={vehicleNumber:v.vehicleNumber,vehicleTypeId:Number(v.vehicleTypeId),currentStationId:Number(v.currentStationId),status:v.status as Vehicle['status'],fuelCardNumber:v.fuelCardNumber||null,registrationExpiry:v.registrationExpiry||null,fitnessExpiry:v.fitnessExpiry||null,insuranceExpiry:v.insuranceExpiry||null,isActive:v.isActive!=='false'};if(mode==='edit')await update.mutateAsync({vehicleId:Number(id),data});else await create.mutateAsync({data});window.location.href='/vehicles';}return <MasterDataPage title="Vehicles" resource="vehicles" mode={mode} rows={rows} detail={item} loading={list.isLoading||detail.isLoading} columns={[{header:'Vehicle',render:r=>r.vehicleNumber},{header:'Type',render:r=>r.vehicleType.name},{header:'Station',render:r=>r.currentStation.name},{header:'Status',render:r=>r.status}]} fields={[{name:'vehicleNumber',label:'Vehicle number'},{name:'vehicleTypeId',label:'Vehicle type ID',type:'number'},{name:'currentStationId',label:'Current station ID',type:'number'},{name:'status',label:'Status',options:Object.values(VehicleStatus).map(v=>({label:v.replaceAll('_',' '),value:v}))},{name:'fuelCardNumber',label:'Fuel card number'},{name:'registrationExpiry',label:'Registration expiry',type:'date'},{name:'fitnessExpiry',label:'Fitness expiry',type:'date'},{name:'insuranceExpiry',label:'Insurance expiry',type:'date'},{name:'isActive',label:'Active',options:[{label:'Active',value:'true'},{label:'Inactive',value:'false'}]}]} schema={z.object({vehicleNumber:z.string().min(1),vehicleTypeId:z.string().min(1),currentStationId:z.string().min(1),status:z.string().min(1),fuelCardNumber:z.string(),registrationExpiry:z.string(),fitnessExpiry:z.string(),insuranceExpiry:z.string(),isActive:z.string()})} initial={initial} onSubmit={submit}/>;}
