{
  "name": "@ems/db",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "migrate": "tsx src/migrate.ts",
    "generate-types": "dotenv -e ../../.env -- kysely-codegen --out-file src/types.ts"
  },
  "dependencies": {
    "dotenv": "latest",
    "kysely": "latest",
    "pg": "latest"
  },
  "devDependencies": {
    "@types/pg": "latest",
    "dotenv-cli": "^11.0.0",
    "kysely-codegen": "latest",
    "tsx": "latest",
    "typescript": "^5.7.3"
  }
}
